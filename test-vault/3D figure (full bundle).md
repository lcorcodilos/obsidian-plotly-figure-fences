# 3D figure (full bundle)

A `scatter3d` trace. This only draws now that the plugin bundles the full
`plotly.js-dist` distribution instead of the cartesian-only build — it's the
one case that exercises the reason for that change (plan §6).

```plotly
figure: 3d_example.json
alt: A 3D scatter plot of x, y=x^2, z=x^3 for x from 1 to 5.
```
